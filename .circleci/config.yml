version: 2
jobs:
  build:
    docker:
      - image: "cimg/node:22.3.0"
    steps:
      - checkout
      - run:
          name: install
          command: npm install --legacy-peer-deps
      - run:
          name: release
          command: npm run semantic-release || true
